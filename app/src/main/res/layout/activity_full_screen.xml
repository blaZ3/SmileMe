<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".app.screens.fullScreen.FullScreenActivity">

    <ImageView
        android:id="@+id/imgFullScreenImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:src="@drawable/place_holder"/>

    <TextView
        android:id="@+id/txtFullScreenImageDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/white"
        android:textSize="16sp"
        android:textAppearance="@style/TextSecondary"
        android:background="@drawable/background_gradient"
        android:padding="@dimen/paddingHalf"
        android:layout_gravity="bottom"
        tools:text="Lorem Ipsum"/>

</FrameLayout>